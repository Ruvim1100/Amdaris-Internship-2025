namespace FileSystemAssessmentProject.Constants
{
    internal static class LogConstants
    {
        public const string logFormat = "Date: {0} || [{1}] || <{2}> || Is Succes: {3} || message: {4}";
    }
}
